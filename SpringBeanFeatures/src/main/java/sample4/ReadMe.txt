Starting from Spring 3.0, Spring offers support for JSR-330 standard annotations (Dependency Injection). 
Those annotations are scanned in the same way as the Spring annotations. 

Dependency Injection with @Inject and @Named

1.Instead of @Autowired, javax.inject.Inject may be used as follows:

